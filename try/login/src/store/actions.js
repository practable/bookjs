export const login = ({ commit }) => {
  commit("login");
};

export const clear = ({ commit }) => {
  commit("clear");
};
export const deleteToken = ({ commit }) => {
  commit("deleteToken");
};

export const deleteBearer = ({ commit }) => {
  commit("deleteBearer");
};
